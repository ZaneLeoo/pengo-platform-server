# Copy Spring's @Qualifier annotation into Lombok-generated constructor parameters.
# Without this, @Qualifier("beanName") on a field is silently dropped from the generated
# constructor, causing Spring to inject the wrong bean when multiple candidates exist.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
