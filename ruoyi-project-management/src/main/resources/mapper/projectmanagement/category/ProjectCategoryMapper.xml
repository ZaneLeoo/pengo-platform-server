<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.projectmanagement.category.mapper.ProjectCategoryMapper">

    <resultMap id="ProjectCategoryResult" type="ProjectCategory">
        <id property="categoryId" column="category_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="ancestors" column="ancestors"/>
        <result property="categoryCode" column="category_code"/>
        <result property="categoryName" column="category_name"/>
        <result property="orderNum" column="order_num"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectCategoryVo">
        select category_id, parent_id, ancestors, category_code, category_name, order_num, status,
               create_by, create_time, update_by, update_time, remark
        from pm_project_category
    </sql>

    <select id="selectProjectCategoryList" parameterType="ProjectCategory" resultMap="ProjectCategoryResult">
        <include refid="selectCategoryVo"/>
        <where>
            <if test="categoryCode != null and categoryCode != ''">and category_code like concat('%', #{categoryCode}, '%')</if>
            <if test="categoryName != null and categoryName != ''">and category_name like concat('%', #{categoryName}, '%')</if>
            <if test="status != null and status != ''">and status = #{status}</if>
            <if test="parentId != null">and (category_id = #{parentId} or parent_id = #{parentId}
                or concat(',', ancestors, ',') like concat('%,', #{parentId}, ',%'))</if>
        </where>
        order by parent_id, order_num, category_id
    </select>

    <select id="selectProjectCategoryById" resultMap="ProjectCategoryResult">
        <include refid="selectCategoryVo"/>
        where category_id = #{categoryId}
    </select>

    <select id="selectProjectCategoryByCode" resultMap="ProjectCategoryResult">
        <include refid="selectCategoryVo"/>
        where category_code = #{categoryCode} limit 1
    </select>

    <select id="countChildren" resultType="int">
        select count(1) from pm_project_category where parent_id = #{categoryId}
    </select>

    <select id="countProjectReferences" resultType="int">
        select count(1) from pm_project where category_id = #{categoryId}
    </select>

    <insert id="insertProjectCategory" useGeneratedKeys="true" keyProperty="categoryId">
        insert into pm_project_category(parent_id, ancestors, category_code, category_name, order_num,
                                        status, remark, create_by, create_time)
        values(#{parentId}, #{ancestors}, #{categoryCode}, #{categoryName}, #{orderNum},
               #{status}, #{remark}, #{createBy}, sysdate())
    </insert>

    <update id="updateProjectCategory">
        update pm_project_category
        set parent_id = #{parentId}, ancestors = #{ancestors}, category_code = #{categoryCode},
            category_name = #{categoryName}, order_num = #{orderNum}, status = #{status},
            remark = #{remark}, update_by = #{updateBy}, update_time = sysdate()
        where category_id = #{categoryId}
    </update>

    <delete id="deleteProjectCategoryById">
        delete from pm_project_category where category_id = #{categoryId}
    </delete>
</mapper>
