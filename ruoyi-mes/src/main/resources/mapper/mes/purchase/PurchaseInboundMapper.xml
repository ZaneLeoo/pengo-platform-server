<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mes.purchase.mapper.PurchaseInboundMapper">

    <resultMap id="InboundResult" type="com.ruoyi.mes.purchase.domain.PurchaseInbound">
        <id property="id" column="id"/>
        <result property="inboundCode" column="inbound_code"/>
        <result property="inboundDate" column="inbound_date"/>
        <result property="status" column="status"/>
        <result property="totalQuantity" column="total_quantity"/>
        <result property="billType" column="bill_type"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <select id="selectList" resultMap="InboundResult">
        select * from purchase_inbound
        <where>
            <if test="inboundCode != null and inboundCode != ''">
                and inbound_code like concat('%', #{inboundCode}, '%')
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
        order by id desc
    </select>

    <select id="selectById" resultMap="InboundResult">
        select * from purchase_inbound where id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into purchase_inbound(inbound_code, inbound_date,
                                     status, total_quantity, bill_type, remark,
                                     create_by, create_time)
        values (#{inboundCode}, #{inboundDate},
                #{status}, #{totalQuantity}, #{billType}, #{remark},
                #{createBy}, now())
    </insert>

    <update id="update">
        update purchase_inbound
        set inbound_code  = #{inboundCode},
            inbound_date  = #{inboundDate},
            status        = #{status},
            total_quantity = #{totalQuantity},
            bill_type      = #{billType},
            remark        = #{remark},
            update_by     = #{updateBy},
            update_time   = now()
        where id = #{id}
    </update>

    <delete id="deleteByIds">
        delete from purchase_inbound where id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectLines" resultType="com.ruoyi.mes.purchase.domain.PurchaseInboundLine">
        select * from purchase_inbound_line where inbound_id = #{inboundId} order by line_no
    </select>

    <insert id="insertLine" useGeneratedKeys="true" keyProperty="id">
        insert into purchase_inbound_line(inbound_id, line_no,
                                          source_receipt_id, source_receipt_code,
                                          source_receipt_line_id, source_receipt_line_no,
                                          source_order_id, source_order_code,
                                          source_order_line_id, source_order_line_no,
                                          material_id, material_code, material_name,
                                          spec, unit, inbound_quantity, lot_no,
                                          warehouse_code, location_code,
                                          remark, create_by, create_time)
        values (#{inboundId}, #{lineNo},
                #{sourceReceiptId}, #{sourceReceiptCode},
                #{sourceReceiptLineId}, #{sourceReceiptLineNo},
                #{sourceOrderId}, #{sourceOrderCode},
                #{sourceOrderLineId}, #{sourceOrderLineNo},
                #{materialId}, #{materialCode}, #{materialName},
                #{spec}, #{unit}, #{inboundQuantity}, #{lotNo},
                #{warehouseCode}, #{locationCode},
                #{remark}, #{createBy}, now())
    </insert>

    <delete id="deleteLines">
        delete from purchase_inbound_line where inbound_id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
