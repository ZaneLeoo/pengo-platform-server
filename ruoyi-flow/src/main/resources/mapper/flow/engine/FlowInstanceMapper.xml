<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.flow.engine.mapper.FlowInstanceMapper">

    <resultMap id="instance" type="com.ruoyi.flow.engine.domain.FlowInstance">
        <id property="instanceId" column="instance_id"/>
        <result property="flowId" column="flow_id"/>
        <result property="flowKey" column="flow_key"/>
        <result property="flowName" column="flow_name"/>
        <result property="bizType" column="biz_type"/>
        <result property="bizId" column="biz_id"/>
        <result property="bizCode" column="biz_code"/>
        <result property="bizName" column="biz_name"/>
        <result property="currentNodeId" column="current_node_id"/>
        <result property="currentNodeName" column="current_node_name"/>
        <result property="status" column="status"/>
        <result property="submitBy" column="submit_by"/>
        <result property="submitName" column="submit_name"/>
        <result property="submitTime" column="submit_time"/>
        <result property="finishTime" column="finish_time"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="vo">
        select instance_id, flow_id, flow_key, flow_name, biz_type, biz_id, biz_code, biz_name,
               current_node_id, current_node_name, status, submit_by, submit_name, submit_time,
               finish_time, create_time, update_time
        from pm_flow_instance
    </sql>

    <select id="selectList" parameterType="com.ruoyi.flow.engine.domain.FlowInstance" resultMap="instance">
        <include refid="vo"/>
        <where>
            <if test="bizType != null and bizType != ''">and biz_type = #{bizType}</if>
            <if test="bizId != null">and biz_id = #{bizId}</if>
            <if test="status != null and status != ''">and status = #{status}</if>
            <if test="flowId != null">and flow_id = #{flowId}</if>
            <if test="submitBy != null and submitBy != ''">and submit_by = #{submitBy}</if>
            <if test="bizName != null and bizName != ''">and biz_name like concat('%', #{bizName}, '%')</if>
        </where>
        order by instance_id desc
    </select>

    <select id="selectById" resultMap="instance">
        <include refid="vo"/>
        where instance_id = #{instanceId}
    </select>

    <select id="selectByBiz" resultMap="instance">
        <include refid="vo"/>
        where biz_type = #{bizType} and biz_id = #{bizId}
        order by instance_id desc limit 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="instanceId">
        insert into pm_flow_instance(flow_id, flow_key, flow_name, biz_type, biz_id, biz_code, biz_name,
                                     current_node_id, current_node_name, status, submit_by, submit_name,
                                     submit_time, create_time)
        values(#{flowId}, #{flowKey}, #{flowName}, #{bizType}, #{bizId}, #{bizCode}, #{bizName},
               #{currentNodeId}, #{currentNodeName}, #{status}, #{submitBy}, #{submitName},
               #{submitTime}, sysdate())
    </insert>

    <update id="updateCurrentNode">
        update pm_flow_instance
        set current_node_id = #{currentNodeId}, current_node_name = #{currentNodeName},
            status = #{status}, update_time = sysdate()
        where instance_id = #{instanceId}
    </update>

    <update id="updateFinish">
        update pm_flow_instance
        set current_node_id = #{currentNodeId}, current_node_name = #{currentNodeName},
            status = #{status}, finish_time = #{finishTime}, update_time = sysdate()
        where instance_id = #{instanceId}
    </update>
</mapper>
